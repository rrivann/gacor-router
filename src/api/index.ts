// HTTP routes (stub). Plan:
// - POST /v1/chat/completions  (OpenAI-compatible)
// - POST /v1/messages          (Anthropic-compatible)
// - GET  /v1/models
// - GET  /health
// - /api/* management endpoints for the dashboard later

export {};
